context:
  version: "0.5.0"
  name: fastapi-zitadel-auth
  python_min: '3.10'

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/f/${{ name }}/fastapi_zitadel_auth-${{ version }}.tar.gz
    sha256: 18d9fbd19de06ae487d947003b78a2d4c45b02784176a366dbe9904c9e3e2a2e

build:
  script: ${{ PYTHON }} -m pip install .
  noarch: python
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - cryptography >=43.0.3
    - fastapi >=0.115.4
    - httpx >=0.27.2
    - pyjwt >=2.12.0

tests:
  - python:
      python_version: ${{ python_min }}.*
      imports:
        - fastapi_zitadel_auth
      pip_check: true

about:
  summary: Zitadel authentication for FastAPI
  license: MIT
  homepage: https://github.com/cleanenergyexchange/fastapi-zitadel-auth
  license_file: LICENCE

extra:
  recipe-maintainers:
    - "0xbe7a"
